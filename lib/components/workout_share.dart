import 'package:flutter/material.dart';

class WorkoutShare extends StatelessWidget {
  const WorkoutShare({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
